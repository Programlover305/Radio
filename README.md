# Radio
What this does is that there's a list of sound effects in the array that when the radio is click is plays a random sound effect. This only has two that I downloaded online which I think is enough to test.
